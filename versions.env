# Source of truth for toolchain versions (repo-wide).
# Consumed via: source versions.env / Docker Compose (.devcontainer/.env symlink).
#
# Step 0 bootstrap: lean image for developing this shared Devinfra repo.
# Issue #10 will expand this into the full product-shared toolchain.

# ── Dev Container base ────────────────────────────────────────────────────────
DEVCONTAINER_BASE_IMAGE=mcr.microsoft.com/devcontainers/base:2.1.15-trixie

# ── Python / uv (quality scripts + agent GitHub CLI; issues #7, #16) ───────────
PYTHON_VERSION=3.12.14
UV_VERSION=0.12.9

# ── Dev Container toolchain ───────────────────────────────────────────────────
STARSHIP_VERSION=v1.25.1
HADOLINT_VERSION=v2.12.0
YQ_VERSION=v4.48.1
NODE_VERSION=v22.23.2
OPENSPEC_VERSION=1.7.0
PRETTIER_VERSION=3.9.6
MARKDOWNLINT_CLI2_VERSION=0.23.2
